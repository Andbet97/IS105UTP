#lang racket
#|Hacer una función que reciba una cadena de caracteres y un carácter adicional.
La cadena se debe retornar con el carácter anexaado al final de la misma.|#

(display "ingrese la cadena ")
(define cadena (read-line))
(display "ingrese el anexo ")
(define z (read-char))
(display cadena)(display z)

